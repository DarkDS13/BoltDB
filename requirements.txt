numpy>=1.24

# GPU backends (install ONE based on your hardware):
# Apple Silicon Mac (uses Metal GPU) — RECOMMENDED for this machine:
mlx>=0.29

# NVIDIA GPU (uncomment if on a CUDA machine):
# cupy-cuda12x
